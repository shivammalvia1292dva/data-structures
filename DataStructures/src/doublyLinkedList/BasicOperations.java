package doublyLinkedList;

//Add to front, add to last , add after a node, delete a node
public class BasicOperations {
    //take care if next node is null then in that case you dont set up the next to previous link
}
